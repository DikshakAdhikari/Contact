import React, {FC} from 'react'
import { useAppSelector } from '../store/store'

export const MapsCharts : FC = ()=> {
    // const contacts= useAppSelector(state=> state.contacts)
    // console.log(contacts);
    
    return (
        <div>
            dfddfd
        </div>
    )
}